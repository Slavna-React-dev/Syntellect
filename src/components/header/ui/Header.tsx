import s from './style.module.css'

const Header = () => {
    return (
        <header className={s.wrap}>
          <h1 className={s.headerTitle}>Syntellect</h1>
        </header>
    )
}

export default Header
